Check assignment readiness:
1. Is golden_set.json populated? How many questions?
2. Is adversarial.json populated? How many questions?
3. Run `python evals/runner.py --quick` and report results
4. Are all src/ files syntax-valid?
5. Can the API start? (`python -c "from src.api import app; print('API OK')"`)

Report as a simple checklist with READY / NOT READY for each.
